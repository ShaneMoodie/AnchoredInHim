@model IEnumerable<AnchoredinHim.Models.Archive>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Blog.Post)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DailyDevotional.Devo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Event.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Blog.Post)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DailyDevotional.Devo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Event.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArchivesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArchivesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArchivesID })
        </td>
    </tr>
}

</table>
